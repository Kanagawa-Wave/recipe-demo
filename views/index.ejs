<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Meal Finder</title>
  <link rel="stylesheet" href="stylesheets/style.css" />
</head>

<body>
  <h1 class="header">Meal Finder</h1>

  <form method="GET">
    <input class="input"
           type="text"
           name="ingredient"
           placeholder="Enter an ingredient…"
           value="<%= ingredient === 'chicken_breast' ? '' : ingredient %>" />
    <button type="submit">Search</button>
  </form>

  <p class="subtitle">
    Main Ingredient: <strong><%= ingredient %></strong>
  </p>

  <% if (meals.length === 0) { %>
      <p>No meals found.</p>
  <% } else { %>
      <% meals.forEach(meal => { %>
        <div class="card">
          <span class="meal-title"><%= meal.strMeal %></span>
          <img src="<%= meal.strMealThumb %>" alt="<%= meal.strMeal %>" />
        </div>
      <% }) %>
  <% } %>
</body>
</html>
